# Configs

Model and runtime configuration files for the AI-Analyst.

## Files

| File | Purpose |
|------|---------|
| `model.json` | Model ID, temperature, max tokens, etc. |
| `env.json` | Environment-specific overrides |
